# AtLoc на train_v2_scene_clean (для сравнения с PoseNet)
# Train: python train.py --config configs/train_v2_atloc.yaml

data:
  type: "colmap"
  root: "train_v2_scene_clean"
  image_size: 224
  train_split: 0.8
  normalize: false
  num_workers: 0

model:
  name: "atloc"
  params:
    backbone: "resnet34"
    pretrained: true
    unfreeze_last_n: 2
    dropout: 0.3
    hidden_dim: 512

training:
  epochs: 80
  batch_size: 16
  lr: 1.0e-4
  weight_decay: 1.0e-5
  scheduler: "cosine"
  warmup_epochs: 3
  patience: 15
  amp: false
  force_cpu: true

output:
  dir: "outputs_clean"
  save_best: true
  tensorboard: true
